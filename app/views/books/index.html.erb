<%= flash[:notice] if flash[:notice] %>

<table id="books">
	<thead>
	<tr>
		<th scope="col">Book</th>
	</tr>
	</thead>
	<tbody>
	<% @books.each do |book| %>
		<tr>
			<td><%= book.name %></td>
		</tr>
	<% end %>
	</tbody>
</table>

<%= link_to 'New Book', new_book_path %>